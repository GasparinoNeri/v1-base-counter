{
  "name": "v1-base-counter",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build:electron": "tsc",
    "build:angular": "cd renderer/app && ng build",
    "start": "npm run build:electron && npm run build:angular && electron ."
  },
  "devDependencies": {
    "electron": "^41.1.1",
    "typescript": "^5.0.0"
  }
}